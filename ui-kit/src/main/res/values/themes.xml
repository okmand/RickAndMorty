<resources>
    <!-- Base application theme -->
    <style name="Theme.RickAndMorty" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color -->
        <item name="colorPrimary">@color/cyan</item>
        <item name="colorPrimaryVariant">?attr/backgroundContentColor</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- StatusBar and NavigationBar color -->
        <item name="android:navigationBarColor">?attr/colorPrimaryVariant</item>
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>
        <item name="android:windowLightStatusBar">true</item>

        <item name="alertDialogTheme">@style/Theme.RickAndMorty.AlertDialogTheme</item>

        <!-- Custom color -->
        <item name="backgroundContentColor">@color/cloud</item>
        <item name="foregroundContentColor">@color/white</item>

        <item name="progressBarColor">?attr/colorOnPrimary</item>
        <item name="progressBarBackgroundColor">?attr/colorPrimary</item>

    </style>

    <style name="Theme.RickAndMorty.AlertDialogTheme" parent="ThemeOverlay.MaterialComponents.Dialog.Alert">
        <item name="buttonBarPositiveButtonStyle">@style/PositiveButtonStyle</item>
        <item name="android:background">?attr/foregroundContentColor</item>
        <item name="android:textColor">@android:color/tab_indicator_text</item>
    </style>

    <style name="PositiveButtonStyle" parent="Widget.MaterialComponents.Button.TextButton.Dialog">
        <item name="android:textColor">@color/cyan</item>
    </style>

</resources>
